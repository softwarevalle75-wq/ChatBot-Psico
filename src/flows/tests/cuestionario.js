import {
	procesarTest,
	mostrarMenuTests,
} from './controlTest.js'

export const mostrarMenuTests = () => {
	return mostrarMenuTests();
}

//
export const manejarTest = async (numeroUsuario, respuestas, tipoTest) => {
	return await procesarTest(numeroUsuario, respuestas, tipoTest);
}